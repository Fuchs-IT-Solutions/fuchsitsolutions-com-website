{
  "name": "fuchsitsolutions-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run optimize:images && vite build",
    "preview": "vite preview",
    "optimize:images": "bash optimize-images.sh"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
